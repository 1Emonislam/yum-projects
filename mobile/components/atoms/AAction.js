import React from 'react'
import { Appbar } from 'react-native-paper'
import { Colors } from '@constants'

const AAction = props => <Appbar.Action color={Colors.black} {...props} />

export default AAction
